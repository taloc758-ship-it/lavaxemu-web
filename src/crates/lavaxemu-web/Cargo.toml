[package]
name = "lavaxemu-web"
version.workspace = true
edition.workspace = true
description = "WASM/browser frontend for the LavaX emulator core"
license.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
lavaxemu-core.workspace = true
wasm-bindgen = "=0.2.128"
js-sys = "0.3"
console_error_panic_hook = "0.1"
